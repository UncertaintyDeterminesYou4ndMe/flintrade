logs/
*.log
__pycache__/
.DS_Store
autoresearch-results.tsv

# Credentials — NEVER commit. Single source of truth for LONGBRIDGE_* secrets.
flintrade.env
flintrade.env.local

# Flintrade agent SQLite (runtime state, not versioned)
flintrade.db
flintrade.db-wal
flintrade.db-shm

# venv (memory subsystem deps: lancedb, fastembed)
.venv/
flintrade_lance/
.daemon.lock

# launchd plist contains real credentials — template is com.flintrade.daemon.plist.example
launchd/com.flintrade.daemon.plist

# web SPA build artifacts
web/node_modules/
web/dist/

# market-data payloads — 可由 backtest/download_*.py 按 manifest 重建,不入库
backtest/data/
backtest/data_1m/
backtest/data_1h/*_US.json
backtest/data_1h_2025/*_US.json
backtest/data_playbook/
llm_calls/
